#pragma once
#ifndef __FACTORY_HPP__
#define __FACTORY_HPP__

#include "base.hpp"
#include "add.hpp"
#include "mult.hpp"
#include "div.hpp"
#include "sub.hpp"
#include "pow.hpp"
#include "op.hpp"

using namespace std;

class Factory
{
public:
	Base* parse(char** input, int length)
	{
		
		for (int i = 1; i < length; i++)
		{
		//loop through input	
		}
	
		if()
		{
                //if input is eqial to +...
		}
		else if
		{
		//if input is eqial to -...	
		}
		else if
		{
		//if input is eqial to /...	
		}
		else if
		{
		//if input is eqial to *...	 
		}
		else if
		{
		//if input is eqial to **...
		}
	}
};
#endif	
